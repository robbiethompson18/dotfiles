# dotfiles
Dotfiles and other scripts to use when setting up a computer (ie .zshrc, claude.md).

Designed for Mac.

All you need to do is run `cd bin && sh install.sh` and it will install everything you need. After that, check out new_laptop_setup.md for next steps, primarily mac settings. 
