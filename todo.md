
2) Do more claude settings
    a) api key helper?
    b) fileSuggestion
    c) always thinking enabled
    d) sandbox? 
    e) what are command hooks? 
    f) MCPs - do I want to auto-enable? Which should I set up? 
    g) by default have yolo mode possible when you shift-tab

3) Have install.sh install:
    a) node
    b) Claude
    c) pnpm
    d) Python? 
    e) Can it install things like Cursor, rectangle, etc? 