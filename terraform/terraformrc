# One shared provider cache instead of a copy per .terraform dir (AWS provider is ~700 MB,
# multiplied by every stack in every checkout). The directory must exist; setup-symlinks.sh creates it.
plugin_cache_dir = "$HOME/.terraform.d/plugin-cache"
