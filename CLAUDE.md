# dotfiles

@~/.claude/personal-repo-rules.md

Robbie's dotfiles, synced across machines. `claude/CLAUDE.md` is the global Claude Code config
(symlinked to `~/.claude/CLAUDE.md` by `bin/setup-symlinks.sh`); `claude/personal-repo-rules.md`
holds the conventions that only apply in his personal repos.

## Docs

- [Kinesis keyboard — read before changing macro keys or the keyboard layout](kinesis/README.md)
  — hk→F-key map, v-Drive push workflow, layout file syntax
